{% extends "base.html.j2" %}
{% block body %}
  {% if 'splash_image' in config.irods_zones[g.irods_session.zone]  %}
    <h2>Welcome to your {{ g.irods_session.zone }} active data portal</h2>
    <div class="row">
      <img src="{{ url_for('static', filename=config.irods_zones[g.irods_session.zone]['splash_image']) }}"
           width="70%"
           class="bi-align-center">
    </div>
  {% else %}
    <h2>Welcome to ManGO, your active data portal</h2>
    <div class="row">
      <img src="{{ url_for('static', filename='mango-logo.png') }}"
           width="70%"
           class="bi-align-center">
    </div>
  {% endif %}
  {# <img src="{{ url_for('static', filename='data-repository.webp') }}" width="70%" class="bi-align-center"> #}
  <p>
    {% for path_element in current_path %}{% endfor %}
  </p>
  {# <h5>Test links</h5>
				<ul>
					<li><a href="{{url_for('metadata_schema_form_bp.form_test')}}">Schema form rendering test</a>
</ul> #}
  {# <p>Total objects: {{total_objects}}</p>
<p>Total attributes: {{total_avu}}</p>
<table class="table">
<thead><tr><th>avu name<th><th>count</th><tr></thead>
<tbody>
{% for item in avu_counts %}<tr><td>{{item['name']}}</td><td>{{ item['total']}}</td></tr>{% endfor %}

</tbody>
</table> #}
  {# {% for collection in collections  %}
  <tr>
	<td><i class="bi bi-folder fs-4"></i> {{collection.name}}</td>
	<td><!--{{ collection.__dict__ }}--></td>
	<td></td>
	<td></td>
  </tr>

{% endfor %} #}
  {# {% for data_object in data_objects  %}
  <tr>
	<td><i class="bi bi-file fs-4"></i> {{data_object.name}}</td>
	<td><!--{{ data_object.__dict__ }}--></td>
	<td></td>
	<td></td>
  </tr>


{% endfor %} #}
{% endblock body %}
